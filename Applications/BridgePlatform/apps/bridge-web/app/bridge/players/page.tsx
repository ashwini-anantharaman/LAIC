export default function PlayersPage() {
  return (
    <div className="mx-auto max-w-4xl space-y-3">
      <h1 className="text-2xl font-semibold tracking-tight">
        Players & Configurations
      </h1>
      <p className="text-sm text-neutral-600">
        Your bridge profile, AI player profiles, convention settings and
        generated convention cards — every configured player will show the
        human-readable, source-cited rules it plays by. Lands in Phase 7,
        backed by published knowledge packages (Phase 3).
      </p>
    </div>
  );
}
