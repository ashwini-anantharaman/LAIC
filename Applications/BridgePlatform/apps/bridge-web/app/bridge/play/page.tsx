export default function PlayPage() {
  return (
    <div className="mx-auto max-w-4xl space-y-3">
      <h1 className="text-2xl font-semibold tracking-tight">
        Play & Practice
      </h1>
      <p className="text-sm text-neutral-600">
        Start a table, resume a session, load a board or deal, and choose
        players (human, deterministic AI, BEN). The bridge table lands in
        Phase 5 on top of persistent sessions (Phase 4) and the published
        Beginner Natural rule package (Phase 3).
      </p>
    </div>
  );
}
