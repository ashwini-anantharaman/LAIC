export default function ProgressPage() {
  return (
    <div className="mx-auto max-w-4xl space-y-3">
      <h1 className="text-2xl font-semibold tracking-tight">Progress</h1>
      <p className="text-sm text-neutral-600">
        Recent sessions, bidding/play alignment, system familiarity, and
        mistake patterns — strictly bridge-domain data (domainId = bridge),
        opened in your program/organization context. Lands in Phase 8
        (evaluator + progress signals).
      </p>
    </div>
  );
}
