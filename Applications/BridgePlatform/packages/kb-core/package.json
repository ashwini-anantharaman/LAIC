{
  "name": "@laic/kb-core",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic knowledge-base component: scoped KB instances, a declarative access policy fed by the Access Catalogue (view/author/review/approve/version/derive), and derivation lineage semantics. The shell never names a content domain — item payloads are opaque; bridge is one adapter (see bridge-web lib/kbComponent).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": { "types": "./src/index.ts", "default": "./src/index.ts" }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
