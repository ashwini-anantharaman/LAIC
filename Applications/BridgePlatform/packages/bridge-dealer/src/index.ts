/**
 * @bridge/dealer
 *
 * Constrained deal generation: structural pre-filters plus evaluator-verified teaching-scope filters (e.g. Level 1 sets where no seat's systemic action is 1NT). Deterministic given (seed, spec, package version).
 *
 * Implementation lands in Phase 6 of
 * laicdocs/Bridge_Workstream_Execution_Plan_v1.md. This is a Phase 0 stub
 * establishing the package boundary; do not add implementation here until
 * that phase begins.
 */

export const PACKAGE_NAME = "@bridge/dealer" as const;
