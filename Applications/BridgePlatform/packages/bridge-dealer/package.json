{
  "name": "@bridge/dealer",
  "version": "0.1.0",
  "private": true,
  "description": "Constrained deal generation: structural pre-filters plus evaluator-verified teaching-scope filters (e.g. Level 1 sets where no seat's systemic action is 1NT). Deterministic given (seed, spec, package version).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bridge/config": "workspace:*",
    "@bridge/engine": "workspace:*",
    "@bridge/events": "workspace:*"
  },
  "devDependencies": {
    "@bridge/knowledge": "workspace:*"
  }
}
