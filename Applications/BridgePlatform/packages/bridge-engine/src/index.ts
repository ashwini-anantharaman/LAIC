/**
 * @bridge/engine
 *
 * Game controller, GameState fold, legality helpers, predicate library, and the rule interpreter that consumes published rule packages.
 *
 * Implementation lands in Phase 2 of
 * laicdocs/Bridge_Workstream_Execution_Plan_v1.md. This is a Phase 0 stub
 * establishing the package boundary; do not add implementation here until
 * that phase begins.
 */

export const PACKAGE_NAME = "@bridge/engine" as const;
