{
  "name": "@bridge/challenges",
  "version": "0.1.0",
  "private": true,
  "description": "Challenges v1: the domain model for BBO-style group challenges (challenge, boards, invites, plays, BEN baselines, BEN decision cache), the net-new field-scoring engine (IMPs vs datum / matchpoints / total points over the completed-human field), and the persistence seam (InMemory/JSON-file; Postgres in @bridge/pg-stores).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./fileStore": {
      "types": "./src/fileStore.ts",
      "default": "./src/fileStore.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bridge/events": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
