{
  "name": "@bridge/table-config",
  "version": "0.1.0",
  "private": true,
  "description": "Table skins & appearance: the canonical, client-safe read model for the play table's look (five skins, strain tints, colour-override palettes, fan limits, presets, defaults) plus the per-user persistence seam (InMemory/JSON-file; Postgres in @bridge/pg-stores).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./fileStore": {
      "types": "./src/fileStore.ts",
      "default": "./src/fileStore.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
