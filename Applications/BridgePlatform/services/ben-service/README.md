# ben-service

Containerized BEN adapter (Blue Chip table-server protocol + WebSocket relay).
Long-running service — never serverless (Bridge plan §17).

Lands in Phase 11 of `laicdocs/Bridge_Workstream_Execution_Plan_v1.md`. BEN is
an optional player/opponent only, never the constrained learner evaluator
(Bridge plan §4.6).
