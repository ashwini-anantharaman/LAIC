import { BridgeEmbed } from "../components/bridge-embed";

/** Assignments (coach): delegated boards and each learner's progress. */
export default function AssignmentsScreen() {
  return <BridgeEmbed title="Assignments" next="/m/assignments" />;
}
