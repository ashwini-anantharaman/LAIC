import { BridgeEmbed } from "../components/bridge-embed";

/** Assignments (learner): boards their coach delegated to them. */
export default function AssignedScreen() {
  return <BridgeEmbed title="Assignments" next="/m/assigned" />;
}
