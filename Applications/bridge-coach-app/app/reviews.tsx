import { BridgeEmbed } from "../components/bridge-embed";

/** Reviews: the coach's queue of plays their learners sent for feedback. */
export default function ReviewsScreen() {
  return <BridgeEmbed title="Reviews" next="/m/reviews" />;
}
