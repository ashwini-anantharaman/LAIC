{
  "name": "@laic/learner-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared cross-platform type contracts for the LAIC / MindBrainAI Nexus ecosystem: common learner model shapes, platform context, event envelope, and the Nexus->Bridge launch context.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
