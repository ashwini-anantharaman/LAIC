/**
 * The Tutorial V3 authoring skin.
 *
 * Same surfaces, same flow, same copy — the palette and type of the learner
 * reader carried up into the authoring pipeline, so a course developer is not
 * moved between two visual languages while building one tutorial.
 *
 * Sage is the forward action: continue, generate, the step you have finished.
 * Navy is the deliberate one you stop and think about: the active step, submit.
 * Everything else is warm paper and stone text.
 */

export const V3_SAGE = '#4d7c5a';
export const V3_SAGE_DARK = '#3d6349';
/** Panel ground for the tinted callouts (Template, Student preview pages). */
export const V3_SAGE_TINT = '#e9f0ea';
export const V3_SAGE_BORDER = 'rgba(77,124,90,0.45)';

/** The one dark: active rail step, and submit. */
export const V3_NAVY = '#1e2b3d';

/** Page ground behind the authoring column. */
export const V3_PAPER = '#f6f4ef';

export const V3_FONT = "'Nunito', ui-sans-serif, system-ui, sans-serif";
